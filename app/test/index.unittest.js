const assert = require('assert');

describe('Assert true', () => {

  let sandbox;

  before(() => {
  });

  it('Should true', async () => {
    
    assert.ok(true);
    
  });

  after(() => {
  });

});
